import styled from "styled-components"

const Container = styled.div({
    
})

function CategoryItem() {
  return (
    <Container>
            import styled from "styled-components"
            
            const Container = styled.div({
                
            })
    </Container>
  )
}

export default CategoryIContainer
